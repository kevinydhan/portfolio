{
  "compilerOptions": {
    "baseUrl": ".",
    "esModuleInterop": true,
    "jsx": "react",
    "paths": {
      "$components": ["src/components"],
      "$context": ["src/context"],
      "$sections": ["src/sections"],
      "$theme": ["src/theme"],
      "$theme/*": ["src/theme/*"],
      "$typings": ["src/typings"],
      "$utils": ["src/utils"]
    }
  }
}
